// TerminalSession.ts — stub kept for reference.
// The Bun-native implementation manages PTY sessions inline in terminalWS.ts
// using Bun.spawn + Bun's built-in WebSocket API.
// Install node-pty via `bun add node-pty` to upgrade to full PTY support with
// proper terminal emulation (ANSI resize / control characters).
export { };
